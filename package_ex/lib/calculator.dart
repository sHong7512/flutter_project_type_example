class PackageCalculator{
  double plus(num x, num y) => x.toDouble() + y.toDouble();
  double minus(num x, num y) => x.toDouble() - y.toDouble();
  double mul(num x, num y) => x.toDouble() * y.toDouble();
  double sub(num x, num y) => x.toDouble() / y.toDouble();
}